export default function PostLayout({
  children,
}: {
  children: React.ReactElement;
}) {
  return children;
  // return (
    // <div className="flex items-center justify-center h-screen bg-slate-100 bg-gradient-to-br from-[#f7f2d8] from-10 to-[#d8c4e2]">
      // {children}
    // </div>
  // );
}
